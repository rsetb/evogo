# EvoNexus Workspace Configuration — Example / Template
# Copy to workspace.yaml (SQLite mode) or use as seed reference (PG mode).
# In PG mode this file is seeded into runtime_configs by migration 0010.

workspace:
  name: "My Workspace"
  owner: ""
  company: ""
  timezone: "America/Sao_Paulo"
  language: "pt-BR"

dashboard:
  port: 8080

chat:
  trustMode: true

agents: {}
integrations: {}
